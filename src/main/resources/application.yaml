spring:
  application:
    name: SiberianSeaBattle
  datasource:
    url: jdbc:h2:mem:siberian_sea_battle
    driver-class-name: org.h2.Driver
    username: username
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: always